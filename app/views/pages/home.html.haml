%section#home_intro
	%h1.text-center.text-white
		Face-to-Face with the
		%br Right Counselor for You


	%form.home_search_wrapper
		%h4.text-center.text-white
			%em
				What kind of counseling do you need?
		.row
			.col-sm-8
				%select.form-control
					= options_for_select(@specialties.map {|s| s.name})
			.col-sm-4
				= link_to "Find a Counselor", counselors_path, :class => "btn btn-primary btn-dark btn-block"

:javascript
	$("nav").addClass("nav-special");

	function setDimensions(){
		var windowsHeight = $(window).height();
		$('#home_intro').css('height', windowsHeight + 'px');
	}

	setDimensions();

	//when resizing the site, we adjust the heights of the sections
	$(window).resize(function() {
		setDimensions();
	});
