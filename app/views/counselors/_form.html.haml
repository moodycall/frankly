= simple_form_for(@counselor) do |f|
  = f.error_notification

  - if current_user.is_admin?
    .form-admin-options
      .form-title
        Admin Options
        %span.small
          (Only visible to admin users)
      .row
        .col-sm-6
          = f.label :hourly_rate_in_cents, :label => "Hourly Rate"
          .input-group
            = f.input :hourly_rate_in_cents, input_html: { :class => "form-control" }, :label => false
            %span.input-group-addon
              cents
        .col-sm-6
          = f.label :hourly_fee_in_cents, :label => "Hourly Fee"
          .input-group
            = f.input :hourly_fee_in_cents, input_html: { :class => "form-control" }, :label => false
            %span.input-group-addon
              cents
  .form-inputs
    .form-group
      = f.label :bio, :label => "Biography"
      = f.input :bio, :placeholder => "Biography", input_html: { :class => "form-control" }, :label => false
    .form-group
      = f.input :profession_start_date, :placeholder => "Profession Start Date", :label => false, as: :string, input_html: { :class => "form-control floatlabel datepicker" }
    .form-group
      = f.input :photo, :label => "Profile Photo", input_html: { :class => "form-control" }

    .form-group
      = f.association :specialties, input_html: { :class => "form-control" }

    .form-group
      = f.association :counseling_licenses do |license|
        .row
          .col-sm-4
            = license.input :license_number, input_html: { :class => "form-control" }
          .col-sm-4
            = license.input :state, collection: CounselingLicense.states, input_html: { :class => "form-control" },:include_blank => false, :label => "Licensed in"
          .col-sm-4
            = license.input :established_on_date, as: :string, input_html: { :class => "form-control datepicker" }, :label => "Licensed Since"

    %hr

    .row
      .col-md-6
        .form-group
          = f.label :send_session_sms_alerts, :label => "Send Session Text Alerts"
          = f.input :send_session_sms_alerts, input_html: { :class => "boolean-switch" }, :label => false
      .col-md-6
        .form-group
          = f.label :send_session_email_alerts, :label => "Send Session Email Alerts"
          = f.input :send_session_email_alerts, input_html: { :class => "boolean-switch" }, :label => false

    %hr
    
  .form-actions
    = f.submit "Update Profile", :class => "btn btn-primary btn-block btn-lg"
